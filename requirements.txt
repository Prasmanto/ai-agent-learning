# Core — used in lessons 1-3
openai>=1.30.0          # Talk to the OpenAI API (the LLM)
python-dotenv>=1.0.0    # Load OPENAI_API_KEY from a .env file
requests>=2.31.0        # Call crypto APIs (CoinGecko) from our tools

# Coming later — uncomment when you reach those lessons
# langchain>=0.2.0
# langchain-openai>=0.1.0
# chromadb>=0.5.0           # Vector DB for RAG (lesson 5)
# pypdf>=4.0.0              # Read whitepapers (lesson 5)
# web3>=6.15.0              # On-chain reads (lesson 4+)
# streamlit>=1.35.0         # Web UI (lesson 7)

# ---------------------------------------------------------------------------
# Dependencies are grouped by the lesson that introduces them.
# You can `pip install -r requirements.txt` once and be ready for all lessons.
# ---------------------------------------------------------------------------

# Lessons 1-3 (core)
openai>=1.30.0          # Talk to the OpenAI API (the LLM)
python-dotenv>=1.0.0    # Load OPENAI_API_KEY from a .env file
requests>=2.31.0        # HTTP calls from our tools (CoinGecko, news APIs)

# Lesson 4 - on-chain tools
web3>=6.15.0            # Read Ethereum data (wallet balances, contracts)

# Lesson 5 - RAG (Retrieval-Augmented Generation)
chromadb>=0.5.0         # Local vector database (stores embeddings)
tiktoken>=0.7.0         # Token counting / chunking utilities

# Lesson 7 - UI
streamlit>=1.35.0       # Simple browser chat UI for the agent
